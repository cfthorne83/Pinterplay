import { connect } from "react-redux";

import AddPinForm from "./add_pin_form";

const msp = (state, ownProps) => {
    return {

    }
}

const mdp = (state, ownProps) => {
    return {

    }
}

export default connect(msp, mdp)(AddPinForm);